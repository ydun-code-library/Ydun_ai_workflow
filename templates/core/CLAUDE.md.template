# Claude AI Assistant Instructions

<!--
TEMPLATE_VERSION: 2.0.0
TEMPLATE_SOURCE: templates/core/CLAUDE.md.template
RULE: keep this file under 200 lines. It loads every turn. Anything Claude can read from the code or from AGENTS.md on demand does not belong here.
-->

**AGENTS.md** holds the project context, stack, commands and status. Read it at the start of a task. Claude Code does not load it automatically when this file exists, so this file says what must be in every turn and points at the rest.

Keep the eight principles and your global rules in `~/.claude/rules/principles.md` and `~/.claude/CLAUDE.md` so they load every turn; do not repeat them here.

## This project

- **What it is**: [ONE_LINE]
- **Stack**: [STACK]
- **Status**: [STATUS] (see `STATUS.md`)
- **Profile**: personal | work (separate Claude Code profile) - [DELETE ONE]

## Commands Claude cannot guess

```bash
[DEV_COMMAND]          # Start dev server
[TEST_COMMAND]         # Run tests (prefer single tests while iterating)
[BUILD_COMMAND]        # Build for production
[TYPECHECK_COMMAND]    # Type checking
[LINT_COMMAND]         # Linting
```

## Conventions that differ from defaults

- [CONVENTION_1: e.g. pnpm not npm]
- [CONVENTION_2: e.g. branch naming, PR conventions]

## Gotchas

- [GOTCHA_1: non-obvious behaviour, required env vars, known traps]

## Jimmy's Workflow

🔴 PRE-FLIGHT → 🔴 IMPLEMENT → 🟢 VALIDATE → 🔵 CHECKPOINT, for all work. VALIDATE names the runnable check. HIGH proceed | MEDIUM human spot-check | LOW stop. Full system: `JIMMYS-WORKFLOW.md` (v2.1).

## Where things go

| Need | Put it in |
|------|-----------|
| Rule Claude got wrong twice | here |
| Rule for some files only | `.claude/rules/<topic>.md` with `paths:` |
| Playbook pasted three times | `.claude/skills/<name>/SKILL.md` |
| Worker spawned repeatedly | `.claude/agents/<name>.md` |
| Must happen every time | hook in `.claude/settings.json` |
| Background, stack, status | `AGENTS.md`, `STATUS.md`, `DOCS-MAP.md` |

## Housekeeping and template audit

- "let's do housekeeping" → read `HOUSEKEEPING.md`, run the checklist, update its dates.
- "check templates" / "are we up to date" → run `path/to/Ydun_ai_workflow/templates/tools/audit-project.sh`, report, offer remediation. Do it; do not just suggest it.
- Navigation: `DOCS-MAP.md` first when asked "where is X documented?"

## GitHub

`gh` for everything: `gh pr create`, `gh pr checks`, `gh issue list`, `gh run watch`.

## Never

- Proceed without GREEN validation. Assume instead of verify. Marketing language in docs. Features nobody asked for. "Fix later" without a written reason.

---

*Last updated: [YYYY-MM-DD]* · *Template Version: 2.0.0*
